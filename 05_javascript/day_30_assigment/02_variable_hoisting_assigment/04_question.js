//=> d. Try writing a function expression before initialization and call it: // Write what happened and why.

hello()
var hello = function() { console.log("Hi") } //becaue here the function is a expression and we cant hoist the function before it intilization when the function is a expression